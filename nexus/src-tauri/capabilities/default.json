{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core events + native file dialogs. All disk I/O goes through the `rpc` command.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
